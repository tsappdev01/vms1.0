# Build output.
.gradle/
build/
*/build/
.kotlin/

# Build logs. Written by the `*> build.log` and `--debug` invocations in README.md, and
# committed by accident once. A --debug log in particular echoes the whole build
# environment - which is nothing today and would be the API key the moment one is set in a
# gradle.properties.
*.log
local.properties
.idea/
*.iml
*.apk
*.aab

# ICP's configuration bundle, which includes the licence file issued to Dubai
# Investments. It is not ours to publish; it is copied in at build time.
app/src/main/assets/toolkit-config/
