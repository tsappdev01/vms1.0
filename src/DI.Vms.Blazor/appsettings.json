{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Vms": "Server=UATWEB01;Database=VMS;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=True"
  },
  "Toolkit": {
    "//Enabled": "false on a host with no card reader - the desk then types the details in. See docs/deployment.md.",
    "Enabled": true,
    "//": "Must contain config_li and config_ag. Set this to your machine's path.",
    "ConfigDirectory": "",
    "LogDirectory": "",
    "//NativeDirectory": "Only needed if the native toolkit DLLs are not beside the app; the build copies them there.",
    "NativeDirectory": ""
  }
}
